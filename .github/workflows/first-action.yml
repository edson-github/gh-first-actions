# This is a basic first workflow to help you get started with Actions

name: First Workflow
on: workflow_dispatch
jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World" | echo "First output"
      - name: Print goodbye | echo "Second output"
        run: echo "Done - bye!"
